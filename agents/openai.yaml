interface:
  display_name: "Multi-Session Project Coordinator"
  short_description: "Evaluate development plans for the most efficient worker count, then coordinate approved sessions."
  default_prompt: "Use $multi-session-project-coordinator after drafting the development plan to decide whether multi-session coordination is worthwhile, recommend the most efficient worker count and role split, ask for user confirmation, then supervise worker threads if enabled."
policy:
  allow_implicit_invocation: true
